<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <% include ../partials/stylesheet %>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  	<% include ../partials/header %>

    <% if (message) { %>
      <div class="info "><p> <%= message %></p></div>
   <% } %>

  	<div class="container">
		<div class="page-header m-t-1">
			<h1>Voting app</h1>
		</div>
		<p class="lead">Welcome to homepage</p>
    <p>Here you can vote on polls from list. Or create your own poll.</p>
    <ul>
    <% for(var i=0; i < polls.length; i++) { %>
        <li>
          <%= polls[i].name %>
            <a href='polls/vote/<%= polls[i]._id %>'>
                click to vote
            </a>
            <span > or </span>
            <a href='polls/voteresult/<%= polls[i]._id %>'>
                click to see vote results
            </a>
            
            <% if (currentUser) { %>
            <span > or </span>
            <a href='polls/<%= polls[i]._id %>'>
                click to add poll options
            </a>
            <% } %>
            
        </li>
    <% } %>
    </ul>

	</div>



  <% include ../partials/footer %>
	<% include ../partials/javascript %>
  </body>
</html>
